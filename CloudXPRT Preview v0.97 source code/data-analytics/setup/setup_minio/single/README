If use ClusterIP service, run mc docker on the same node.
 # docker run -it --network="host" --entrypoint=/bin/sh minio/mc

If use NodePort service, run mc docker on a different machine,
 # mc config host add local http://10.3.55.144:31118 minio minio123
mc: Configuration written to `/root/.mc/config.json`. Please update your access credentials.
mc: Successfully created `/root/.mc/share`.
mc: Initialized share uploads `/root/.mc/share/uploads.json` file.
mc: Initialized share downloads `/root/.mc/share/downloads.json` file.
Added `local` successfully.
/ # mc mb local/hahaha
Bucket created successfully `local/hahaha`.

